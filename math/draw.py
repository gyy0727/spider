import matplotlib.pyplot as pltimport numpy as npimport pandas as pdfrom sklearn.cluster import KMeansdf = pd.read_excel(r"C:\Users\mu'qiu\Desktop\数据文件.xls")a = df['region_1']b = df['region_2']c = df['region_3']d = df['region_4']e = df['region_5']f = df['region_6']g = df['region_7']h = df['region_8']a2 = df['day']data1 = np.array(a)data2 = np.array(b)data3 = np.array(c)data4 = np.array(d)data5 = np.array(e)data6 = np.array(f)data7 = np.array(g)data8 = np.array(h)X = np.hstack((data1,data2,data3,data4,data5,data6,data7,data8)).Tprint(min(X))# kmeans = KMeans(n_clusters=6)## # predictions = kmeans.fit_predict(data1.reshape(-1, 1))# predictions = kmeans.fit_predict(X.reshape(-1, 1))# iterations = kmeans.n_iter_# print(iterations)# # 可视化展示# plt.scatter(range(len(predictions)), X.reshape(-1,1), c=predictions)# plt.show()